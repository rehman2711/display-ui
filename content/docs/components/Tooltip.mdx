---
title: Tooltip
description: Provides brief contextual information or hints when hovering or focusing on an element.
---

<ComponentPreview name="TooltipDemo" description=" A Tooltip Component ." />

## Installation

<ComponentInstall>

<CLIInstall>
  
```bash
npx shadcn@latest add https://display-ui.vercel.app/r/Tooltip
```

</CLIInstall>

<ManualInstall>

<Steps>

<Step>

Install the following dependencies

</Step>

```bash
no dependencies
```

<Step>

Copy and paste the following code into your project

</Step>

<ComponentSource title="components/ui/Tooltip.tsx" name="Tooltip" />

<Step>

Update the import paths to match your project setup

</Step>

</Steps>

</ManualInstall>

</ComponentInstall>

## Usage

```tsx
import { Button } from "@/registry/default/displayui/Button"
import { Tooltip } from "@/registry/default/displayui/Tooltip"
```

```tsx
<Tooltip.Root>
  <Tooltip.Trigger>
    <Button>Hover me</Button>
  </Tooltip.Trigger>
  <Tooltip.Content content="I am a tooltip!" position="simple" />
</Tooltip.Root>
```

## API

### Tooltip Props

<Steps>

<Step>
  <p className="font-semibold">For Tooltip Root</p>
</Step>

<APITable
  data={[
    {
      attribute: "children",
      type: "ReactNode | ReactNode[]",
      description:
        "Wraps the tooltip trigger and content. Provides the hover group context required for tooltip visibility.",
      default: "-",
    },
    {
      attribute: "className",
      type: "string",
      description:
        "Additional CSS classes to customize the tooltip root container styling.",
      default: "-",
    },
  ]}
/>

<Step>
  <p className="font-semibold">For Tooltip Trigger</p>
</Step>

<APITable
  data={[
    {
      attribute: "children",
      type: "ReactNode",
      description: "Element that triggers the tooltip on hover.",
      default: "-",
    },
    {
      attribute: "className",
      type: "string",
      description:
        "Additional CSS classes to customize the tooltip trigger styling.",
      default: "-",
    },
  ]}
/>

<Step>
  <p className="font-semibold">For Tooltip Content</p>
</Step>

<APITable
  data={[
    {
      attribute: "content",
      type: "ReactNode",
      description: "Content displayed inside the tooltip.",
      default: "-",
    },
    {
      attribute: "position",
      type: '"top" | "bottom" | "left" | "right" | "simple"',
      description:
        "Controls the placement of the tooltip relative to the trigger.",
      default: '"top"',
    },
    {
      attribute: "className",
      type: "string",
      description:
        "Additional CSS classes to customize the tooltip content styling.",
      default: "-",
    },
  ]}
/>

</Steps>

## Examples

### Simple

<ComponentPreview name="TooltipDefaultDemo" />

### Top

<ComponentPreview name="TooltipTopDemo" />

### Bottom

<ComponentPreview name="TooltipBottomDemo" />

### Left

<ComponentPreview name="TooltipLeftDemo" />

### Right

<ComponentPreview name="TooltipRightDemo" />

## Credits

This component is inspired by [Rehman Kalawant Design Shared On Github](https://github.com/rehmankalawant/display-ui).
